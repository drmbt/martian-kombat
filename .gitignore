# Secrets — never commit
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Node
node_modules/
dist/
.vite/
*.log

# Asset pipeline intermediates. assets/raw/ (generated character frames) is
# now committed so we can hand-fix individual frames later without regenerating
# — it costs some repo size but keeps the source frames versioned.
assets/veo-clips/
assets/frame-dumps/
*.mp4.tmp

# Editor
.vscode/
.idea/
*.swp
